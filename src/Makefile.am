AM_CFLAGS= $(CC_OPT)

SUBDIRS=proto include common communication module-loader ldm dss cfg \
        daemon serializer ldm-modules io io-modules layout store admin lrs \
        layout-modules cli tlc
